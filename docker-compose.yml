services:
  server:
    container_name: klofyi
    image: felixtech/klofyi:latest
    environment:
      GEOAPIFY_KEY: TODO
    ports:
      - "80:3000"
    logging:
      driver: "json-file"
    restart: always
